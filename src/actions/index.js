import { SET_CHANNEL_ID } from "../Constants"

export const setChannelId = (channelId) => dispatch => {
    dispatch({type: SET_CHANNEL_ID, payload: channelId});
}

export const setBids = (bids) => dispatch => {

}

export const setAsks = (asks) => dispatch => {

}

export const updateBids = (updatedBids) => dispatch => {

}

export const updateAsks = (updatedAsks) => dispatch => {

}

